import React from 'react'
//import {connect} from "react-redux"
const ListTask = (tasks) => {
   
    return (
        <div>
            ListTask {tasks}
        </div>
    )
}

export default ListTask
